{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gregrr.github.io/fermentation-json/schemas/0.1.0/core/identity.schema.json",
  "title": "FermentationJSON identity primitives",
  "description": "Reusable identifiers used by FermentationJSON schemas and documents.",
  "$defs": {
    "document_id": {
      "$anchor": "documentId",
      "title": "Document identifier",
      "type": "string",
      "format": "uri",
      "minLength": 1,
      "pattern": "^[A-Za-z][A-Za-z0-9+.-]*:"
    },
    "object_id": {
      "$anchor": "objectId",
      "title": "Document-scoped object identifier",
      "type": "string",
      "minLength": 1
    },
    "schema_id": {
      "$anchor": "schemaId",
      "title": "Schema or profile identifier",
      "type": "string",
      "format": "uri",
      "minLength": 1,
      "description": "Absolute identifier of a normative schema, profile, module, compatibility profile, or other declared conformance artifact.",
      "pattern": "^[A-Za-z][A-Za-z0-9+.-]*:"
    },
    "media_type": {
      "$anchor": "mediaType",
      "title": "Media type",
      "type": "string",
      "minLength": 1
    },
    "checksum": {
      "$anchor": "checksum",
      "title": "Checksum",
      "type": "object",
      "required": [
        "algorithm",
        "value"
      ],
      "properties": {
        "algorithm": {
          "type": "string",
          "minLength": 1
        },
        "value": {
          "type": "string",
          "minLength": 1
        }
      },
      "unevaluatedProperties": false
    }
  }
}
